# purescript-gasket
Gasket is a minimal functional layer for Google Apps Script, written in PureScript. It provides a clean boundary between pure, strongly-typed domain logic and the imperative GAS runtime, with explicit effects, real ADTs, and predictable APIs.
